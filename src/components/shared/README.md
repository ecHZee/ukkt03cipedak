# components/shared/

Komponen lintas area: `Container`, `SectionHeader`, `StatTile`, `Tile`,
`PeriodeBadge`, `StatusDot`, `EmptyState`, dll.

Tidak boleh impor dari `features/public/*` atau `features/admin/*`.
