# lib/

Helper lintas-domain yang lebih berat dari `utils/`: client factory tambahan,
auth middleware kustom, validator skema (Zod), dsb. File `error-page.ts`,
`error-capture.ts`, `lovable-error-reporting.ts`, dan `utils.ts` di sini
sudah dipakai oleh template.
